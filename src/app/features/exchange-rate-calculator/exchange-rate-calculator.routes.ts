import { Routes } from '@angular/router';
import { ExchangeRateCalculatorComponent } from './pages/exchange-rate-calculator.page';

export const EXCHANGE_RATE_CALCULATOR_ROUTES: Routes = [
    {
        path: '',
        component: ExchangeRateCalculatorComponent,
    },
];
